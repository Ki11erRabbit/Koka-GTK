[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "koka-gtk-gen"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["lxml"]

[project.optional-dependencies]
dev = ["pytest", "black"]

[project.scripts]
koka-gtk-gen = "gen.__main__:main"